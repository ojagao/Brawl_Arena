const CONFIG = Object.freeze({
  PORT: process.env.PORT || 3004,
  PUBLIC_URL: process.env.PUBLIC_URL || '',

  TICK_RATE: 60,
  TICK_INTERVAL: 1000 / 60,

  MAX_PLAYERS: 6,
  LOBBY_WAIT_TIME: 30000,
  TEAMS: Object.freeze(['red', 'blue']),
  PLAYERS_PER_TEAM: 3,

  CHARACTER_SELECT_TIME: 30000,
  GAME_DURATION: 120000,
  KILLS_TO_WIN: 10,

  RESPAWN_TIME: 3000,
  HEALTH_REGEN_DELAY: 3000,
  HEALTH_REGEN_RATE: 2,

  MAP_WIDTH: 1600,
  MAP_HEIGHT: 900,
  TILE_SIZE: 40,

  INPUT_SEND_RATE: 30,
  CONTROLLER_STATE_RATE: 20,
  SHOOT_THRESHOLD: 0.3,

  DISCONNECT_TIMEOUT: 30000,
  ROOM_CLEANUP_TIMEOUT: 60000
})

module.exports = CONFIG
